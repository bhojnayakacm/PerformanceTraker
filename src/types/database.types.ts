/**
 * Supabase Database Types (Auto-generated)
 * =========================================
 * This file will be REPLACED in Phase 2 with auto-generated types from:
 *   npx supabase gen types typescript --project-id <your-project-id>
 *
 * For now, we define an empty Database type so TypeScript doesn't complain
 * when the Supabase client files import it. Once we create the actual
 * tables in Supabase, we'll regenerate this file and get full type safety
 * for every query (column names, return types, insert types, etc.).
 */

export type Database = {
  public: {
    Tables: Record<string, never>;
    Views: Record<string, never>;
    Functions: Record<string, never>;
    Enums: Record<string, never>;
  };
};
